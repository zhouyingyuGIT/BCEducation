export const activeMenuName = state => state.activeMenuName

export const activeSiderIndex = state => state.activeSiderIndex

export const msgTotal = state => state.msgTotal

export const checkCodeList = state => state.checkCodeList

export const commitCodeList = state => state.commitCodeList
